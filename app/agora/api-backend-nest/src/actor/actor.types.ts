export enum ActorLifecycleState {
  NEW = 'NEW',
  CHURNED = 'CHURNED',
  QUALIFIED = 'QUALIFIED',
  BLOCKED = 'BLOCKED',
}

export enum ScoreOperator {
  LT = 'lt',
  LTE = 'lte',
  GT = 'gt',
  GTE = 'gte',
  EQ = 'eq',
}
